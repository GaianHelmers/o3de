/*
 * Copyright (c) Contributors to the Open 3D Engine Project.
 * For complete copyright and license terms please see the LICENSE at the root of this distribution.
 *
 * SPDX-License-Identifier: Apache-2.0 OR MIT
 *
 */


.primaryText
{
    color: $PrimaryTextColor;
}

.secondaryText
{
    color: $SecondaryTextColor;
}

.highlightedText
{
    color: $HighlightTextColor;
}

.blackText
{
    color: $BlackTextColor;
}

/*
    Qt supports the line-height field, but only for QTextEdit controls, not for QLabel.
    Even then, the support isn't exactly what we want.
    So instead, we apply margin-top and bottom appropriately to get the required spec.

    To get the right line-height, we need to do:

    margin-top: ((line-height - font-size) / 2) px;
    margin-bottom: ((line-height - font-size) / 2) px;
*/

/*
    Qt Style Sheets provide the font-weight property but it expects the following
    values:
        normal
        | bold
        | 100
        | 200
        ...
        | 900

    Using the QFont::Weight values as guidance, we extrapolate Light to 250.
*/

.Headline
{
    font-family: "$FontFamily";
    font-size: $FontSizeHeadline;
    font-weight: 250;
    margin-top: $SpacingTextHeadlineV;
    margin-bottom: $SpacingTextHeadlineV;
}

.Title
{
    font-family: "$FontFamily";
    font-size: $FontSizeTitle;
    margin-top: $SpacingTextTitleV;
    margin-bottom: $SpacingTextTitleV;
}

.Subtitle
{
    font-family: "$FontFamily";
    font-size: $FontSizeSubtitle;
    font-weight: 250;
    margin-top: $SpacingTextSubtitleV;
    margin-bottom: $SpacingTextSubtitleV;
}

.Menu
{
    font-family: "$FontFamily";
    font-size: $FontSize;
    margin-top: $SpacingTextMenuV;
    margin-bottom: $SpacingTextMenuV;
}

.Label, QLabel
{
    font-family: "$FontFamily";
    font-size: $FontSize;
    margin-top: $SpacingTextLabelV;
    margin-bottom: $SpacingTextLabelV;
}

.Paragraph
{
    font-family: "$FontFamily";
    font-size: $FontSize;

    /*
       line-height doesn't work with multi-line text fields.
       With single-line text fields, we can get close enough using the margin-top and margin-bottom. That doesn't
       work for multi-line fields, because there's no inbetween line margins.
    */

    margin-top: $SpacingTextParagraphV;
    margin-bottom: $SpacingTextParagraphV;
}

.Tooltip
{
    font-family: "$FontFamily";
    font-size: $FontSize;
    margin-top: $SpacingTextTooltipV;
    margin-bottom: $SpacingTextTooltipV;
}

.Button
{
    font-family: "$FontFamily";
    font-size: $FontSize;
    font-weight: 570;
    margin-top: $SpacingTextButtonV;
    margin-bottom: $SpacingTextButtonV;
}
